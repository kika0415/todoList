import React from 'react';
import { TextField } from '@material-ui/core';

const Header = () => {
  return (
    <>
      <TextField label='inout a todo, press enter to add' fullWidth />
    </>
  );
};

export default Header;
